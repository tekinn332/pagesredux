export const addusername = (payload) => ({
    type: "ADD_USERNAME",
    payload,
  });
  
  export const addemail = (payload) => ({
    type: "ADD_EMAIL",
    payload,
  });
  
  export const addpassword = (payload) => ({
    type: "ADD_PASSWORD",
    payload,
  });
  
  export const addconfirmpassword = (payload) => ({
    type: "CONFIRMPASSWORD", 
    payload,
  });
  
  export const addage = (payload) => ({
    type: "AGE",
    payload,
  });
  